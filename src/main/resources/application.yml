server:
  port: 9001

spring:
  application:
    name: script_platform

  datasource:
    url: jdbc:mysql://localhost:3306/script_platform?useUnicode=true&characterEncoding=utf-8&isSSL=false
    username: root
    password: root

  jpa:
    show-sql: true
    open-in-view: false

  liquibase:
    change-log: classpath:/liquibase/changelog-master.xml

logging:
  level:
    org.springframework.security: trace
